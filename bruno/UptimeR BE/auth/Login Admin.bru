meta {
  name: Login Admin
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  mimeType: application/json
}

body:json {
  {
    "email": "{{loginEmailAdmin}}",
    "password": "{{loginPassword}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
