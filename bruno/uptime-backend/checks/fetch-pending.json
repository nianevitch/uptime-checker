{
  "meta": {
    "name": "Fetch Pending Checks",
    "type": "http"
  },
  "request": {
    "url": "{{baseUrl}}/api/checks/pending?count=5",
    "method": "GET",
    "headers": [
      {
        "name": "Authorization",
        "value": "Bearer {{authToken}}"
      }
    ]
  },
  "tests": [
    {
      "name": "Expect OK",
      "script": "client.test(\"Fetched pending checks\", () => {\n  client.assert(response.status === 200, \"Expected 200, got \" + response.status);\n});\n"
    }
  ]
}


