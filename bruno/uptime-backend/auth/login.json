{
  "meta": {
    "name": "Login",
    "type": "http"
  },
  "request": {
    "url": "{{baseUrl}}/api/auth/login",
    "method": "POST",
    "headers": [
      {
        "name": "Content-Type",
        "value": "application/json"
      }
    ],
    "body": {
      "mimeType": "application/json",
      "data": "{\n  \"email\": \"{{loginEmail}}\",\n  \"password\": \"{{loginPassword}}\"\n}"
    }
  },
  "tests": [
    {
      "name": "Store JWT",
      "script": "client.test(\"Login succeeded\", () => {\n  client.assert(response.status === 200, \"Expected HTTP 200, received \" + response.status);\n});\nconst token = response.body.token;\nif (token) {\n  client.global.set(\"authToken\", token);\n}\n"
    }
  ]
}


